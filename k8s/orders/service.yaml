apiVersion: v1
kind: Service
metadata:
  name: orders                   
spec:
  selector:
    app: orders                 
  ports:
    - name: grpc
      port: 8080                    
      targetPort: 8080              
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP